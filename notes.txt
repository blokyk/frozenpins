foo = {
    pins = {
        nixpkgs = v2
        hm = {
            ...
        }
    }
    builtins.follows = {
        hm.nixpkgs = <nixpkgs>
    }
    code = (import <hm>).home
}

import <hm> = {
    file = "hm"
    follows = builtins.follows
    follows.hm.nixpkgs = foo.nixpkgs
    rawPins = {
        nixpkgs = v1
    }
    pins = rawPins // follows
}

hm = {
    pins = {
        nixpkgs = v1
        zoeee = v2
    }
    builtins.follows = {
        zoeee.nixpkgs = <nixpkgs>
    }
    code = (import <zoeee>).hello
}

zoeee = {
    pins = {
        nixpkgs = v0
    }
}
